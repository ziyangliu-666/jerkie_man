# 耐力系统测试指南

## 实现完成的功能

### 1. 服务端耐力系统 ✅
- Player类包含耐力属性：`stamina`, `maxStamina`, `isSprinting`
- processInput方法支持冲刺参数
- 耐力计算使用共享的模拟逻辑

### 2. 共享模拟逻辑 ✅
- `calculateStaminaChange()`: 耐力消耗/恢复计算
- `canSprint()`: 冲刺可行性检查
- `getSprintSpeedMultiplier()`: 冲刺速度倍数 (1.5x)
- 耐力常量：消耗速率 20/s，恢复速率 15/s (静止) 或 5/s (移动)

### 3. 客户端输入处理 ✅
- InputManager支持`getSprintHeld()`方法（空格键）
- 客户端主循环发送冲刺状态到服务端
- 客户端预测包含耐力计算

### 4. 通信协议 ✅
- `C2S_INPUT`包含`sprint`字段
- `PLAYER_STATE`包含耐力相关字段

### 5. DOM UI显示 ✅
- 耐力HUD显示在左下角（生命值和武器状态之间）
- 包含数值显示、进度条和状态文字
- 颜色变化（绿色→橙色→红色）和冲刺闪烁动画
- 与现有的DOM UI系统完全集成

## 测试步骤

1. **启动游戏**
   ```bash
   npm run dev:all
   ```

2. **打开浏览器**
   - 访问 http://localhost:5174/
   - 进入游戏房间

3. **测试基本功能**
   - 按住空格键应该能看到角色移动速度增加
   - 耐力HUD应该在左下角显示（生命值上方）
   - 冲刺时耐力条应该闪烁并逐渐减少

4. **测试耐力机制**
   - 冲刺时耐力消耗：20点/秒
   - 停止冲刺后耐力恢复：
     - 静止时：15点/秒
     - 移动时：5点/秒
   - 耐力耗尽时无法继续冲刺

5. **测试UI显示**
   - 耐力条颜色变化：
     - 绿色：60%以上
     - 橙色：30%-60%
     - 红色：30%以下
   - 冲刺时耐力条闪烁效果
   - 显示"SPRINTING"文字提示
   - 数值显示格式：`当前耐力/最大耐力`

## 预期行为

- **正常冲刺**：按住空格键 + 移动键，角色速度提升1.5倍，耐力逐渐消耗
- **耐力恢复**：松开空格键后，耐力开始恢复
- **耐力耗尽**：耐力为0时无法冲刺，必须等待恢复
- **客户端预测**：本地立即响应冲刺输入，服务端权威验证

## UI布局

```
左下角HUD布局（从下到上）：
┌─────────────────┐
│   武器 HUD      │  ← 最下方
├─────────────────┤
│   耐力 HUD      │  ← 中间（新增）
├─────────────────┤
│   生命值 HUD    │  ← 最上方
└─────────────────┘
```

## 技术细节

### 耐力系统参数
```typescript
const STAMINA_SPRINT_MULTIPLIER = 1.5; // 冲刺速度倍数
const STAMINA_DRAIN_RATE = 20; // 冲刺时耐力消耗速率（点/秒）
const STAMINA_REGEN_IDLE = 15; // 静止时耐力恢复速率（点/秒）
const STAMINA_REGEN_MOVING = 5; // 移动时耐力恢复速率（点/秒）
const STAMINA_MIN_TO_SPRINT = 1; // 开始冲刺所需的最小耐力
```

### DOM UI集成
- 使用现有的DOM HUD系统而非Canvas UI
- CSS样式与生命值HUD保持一致
- 响应式设计，支持不同屏幕尺寸
- 平滑的动画过渡效果

## 故障排除

如果耐力系统不工作，检查：
1. 服务端日志是否显示冲刺输入
2. 客户端是否正确发送sprint参数
3. DOM元素是否正确创建和更新
4. 浏览器控制台是否有错误信息
5. CSS样式是否正确加载